// Stack memory (Used by primitive data types) => Gives a copy and then change the value of the copy.

// Heap memory (Used by non-primitive data types) => Directly changes the value of the reference.